
export enum Action {
    ADD = 'Add',
    UPDATE = 'Update',
}

export enum Status {
    ACTIVE = 'Active',
    INACTIVE = 'Inactive',
}

export enum Gender {
    MALE = 'Male',
    FEMALE = 'Female',
}